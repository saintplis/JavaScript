function soma(n1=0,n2=0){ // A função espera os valores dos parâmetros que serão enviados da chamada
    return n1 + n2 // Soma os parâmetros
}
console.log(soma(2,5)) // Chamada que define os valores dos parâmetros da função (soma)